/**
 * Defines how to encode and decode objects.
 *
 * The objects to be stored into a cache storage like {@code java.nio.ByteBuffer}s, files and so on.
 * And This package also provides base implementations for xml and JSON encoding and decoding to output into
 * or input from the external storage outside the cache.
 */
package net.ihiroky.reservoir.coder;
